---
type: survey-methods
field: RL post-training for video temporal understanding
created: 2026-07-04
updated: 2026-07-04
language: zh-CN
note: 已按需求窄化 —— 只讲 RL 后训练(GRPO / RLVR / OPD)如何提升「时序理解」的机制,不再覆盖架构/长上下文/SFT-grounding/agentic 等非 RL 族。以 SynRL(2603.17693)为主案例。机制讲到 reward 公式层面。
related:
  - ../导读-Video-R1-cn.md                                  # Video-R1 / T-GRPO 深读
  - ../videoOPD/video-推理任务-cn.md                        # RL 后训练 SOTA 盘
  - 03-主流benchmark缺陷剖析-cn.md                          # "shuffle 验证"判据来自 03
  - ../video-任务全景-思维导图-cn.md                        # §8 OPD 统一视角(OPD=RL,reward 换 teacher)
---

# RL 后训练如何提升视频「时序理解」——机制拆解

> **窄化说明**:本篇只回答一件事——**RL 后训练(GRPO/RLVR/OPD)到底靠什么机制让模型"更懂时间"**。架构编码 / 长上下文 / SFT-grounding / agentic 等非 RL 手段不在此篇(需要另开)。主案例:**SynRL**。

---

## 0. ★ 核心命题:为什么"普通 RL 未必提升时序理解"

标准 GRPO/RLVR 只用一个**末端可验证 reward**(答案对/错)。问题在于:**时序题里,模型常能靠静态表观/语言先验答对,根本没用到时间顺序**(见 03:MVBench 打乱帧只掉 ≤1.2%)。于是:

- **普通 RL 优化的是"答对",不是"用时序答对"** → 它可能**强化捷径**,而非提升时序理解。
- 所以"RL 提升时序"的**充要设计** = 让 reward / 数据 / credit **只有在真正利用了时序时才被最大化**。

由此引出两条**前置约束**(对你 OPD 尤其关键):
1. **信号必须"指向时序"**:reward 要么惩罚"乱序也能答对"(对比式),要么要求输出时间戳(grounding),要么把推理钉在时序证据上(一致性)。
2. **信号必须"可靠"**:reward 若来自噪声标注,RL 学到的是噪声。**SynRL 的第一性洞察正在此**(§2)。

> 判据:一个 RL 方法是否**真**提升了时序理解,标准是——**在 shuffle/reverse 敏感盘(TVBench/Vinoground/TOMATO)上涨点**,而非只在 Video-MME/MVBench 涨(那可能是表观增益)。

---

## 1. 机制光谱(六类"时序信号来源")

| # | 机制 | 时序信号从哪来 | 代表 | 真提升? |
|---|---|---|---|---|
| 一 | **可验证的干净时序 reward** | 合成数据的完美 frame-level GT | **★ SynRL** | 🟢🟢(21× 数据效率) |
| 二 | **时序对比 reward** | 正序 vs 乱序帧的正确率差 | Video-R1(T-GRPO) | 🟢 |
| 三 | **grounding IoU reward** | 输出时间戳,tIoU 可验证 | Time-R1, Tempo-R0, TempSamp-R1 | 🟢(定位) |
| 四 | **grounded / 过程一致性 reward** | 推理轨迹 ↔ 时序视觉证据对齐 | VideoRFT, MOSS-ChatV | 🟢(防编故事) |
| 五 | **时序负例 / 自监督** | 乱序/倒放/扰动段作负例 | TPO, Synthetic-Pref, VideoSSR | 🟢(负例干净) |
| 六 | **细粒度 credit assignment** | 把 credit 给"用了时序证据"的 token | TW-GRPO, Video-KTR | 🟡(改进项) |

> 六类不是互斥的——SOTA 常组合(如 SynRL = 一 + 二的思想;VideoRFT = 二 + 四)。下面逐类讲机制。

---

## 2. ★ 机制一:可验证的干净时序 reward —— SynRL(主案例)

> **《Learning Transferable Temporal Primitives for Video Reasoning via Synthetic Videos》**,arXiv **2603.17693**(2026-03)。

**它的洞察(全篇最值得记)**:RL 提升时序的瓶颈**不是算法,是 reward 可靠性**。真实视频的时序标注**由闭源模型生成、会幻觉运动方向/顺序 → 污染 reward → RL 学到系统性错误**。SynRL 用**程序化合成**绕开:

- **怎么生成**:Python 代码 + 物理方程模拟(初始化 shape/color/velocity,迭代更新位置,几何检测碰撞),**帧级时间戳 @30 FPS**,自动导出 state snapshots / 带毫秒时间戳的 operation 序列 / event 列表 → **完美 ground-truth,构造即无误**。
- **时序原语(temporal primitives,它要 instill 的东西)**:
  - **短时感知**(12 类):方向识别、速度比较、轨迹形状、加速度、运动计数 —— 逼**逐帧注意**而非静态模式匹配。
  - **长时认知**(6 类):状态跟踪、回溯推理(backward)、符号操作、隐状态数学 —— 建模 `{S_t, o_t, S_{t+1}}` 只可见首/末态 → **逼内部模拟**(纸牌堆、shell game、滑块)。
- **训练 pipeline**:
  1. **SFT-CoT**:6.7K 合成 CoT(teacher 显式写出带帧时间戳的逐步时序推理)+ 1K 真实 LLaVA-Video QA(**去掉原 CoT 防误差传播**)。
  2. **GRPO**:7K 样本,**reward = 二元准确率**(对合成题,正确答案由模拟日志直接算出)。"合成数据的可验证性 → 完全正确的 reward → 在严格可靠的监督下精炼推理"。
- **关键结果(为什么这机制强)**:
  - **7.7K 合成 CoT > Video-R1 的 165K 真实样本**(21× 数据效率)。
  - 时序定位 RexTime **+12.6%**(38.8 vs 26.2 R@0.3);TOMATO **+4.6%**(36.7 vs 32.1)。
  - Vinoground:**合成训练 +2.1%,而 Video-R1 −4.2%** —— 直接归因于闭源标注的系统性错误。跨 15 盘泛化(几何形状训练 → 真实视频)。

**takeaway(对你 RL/OPD 都是第一性原则)**:**干净、可验证的时序监督信号 > 海量但有噪声的真实数据**。时序理解的 RL 增益上限,由 reward 的正确性而非数据量决定。→ 直通 §8 的 OPD 类比。

---

## 3. 机制二:时序对比 reward —— Video-R1 的 T-GRPO(讲到公式)

**思路**:同一题喂两次——**正序帧**和**随机打乱帧**。只有当"正序比乱序更能答对"时才发时序 bonus,逼模型利用顺序。

**公式**(α=0.3, μ=0.8):

```
时序系数    r_t = α · 𝟙[ p > μ · p̃ ]          # p=正序帧下该组正确率, p̃=乱序帧下正确率
总 reward   r_i^{T-GRPO} = r_i + r_t   若 o_i 正确
                         = r_i         否则     # bonus 只加在答对的响应上
```

组内 advantage 与标准 GRPO 一致(组内归一化)。**直觉**:`p > μ·p̃` 检测"这道题时序是否真被用上了";用上了才奖励 → 把"利用时序"变成显式优化目标。**局限**:bonus 是 **group 级软信号**(μ=0.8 是软阈值),主体仍是最终答案 reward → 增益不如 SynRL 的"逐样本可验证"纯。

**结果**:MVBench 63.9 / TempCompass 73.2 / VSI-Bench 35.8(7B,32 帧)。配 165K 真实 CoT 冷启。

---

## 4. 机制三:grounding IoU reward(定位天然可验证)

**思路**:把时序任务改写成**输出时间区间 [t₁,t₂]**,reward = **tIoU**(+ format)。因为时间戳可与 GT 精确比对,**reward 干净可验证**(这与 SynRL 精神一致,只是靠任务形式而非合成数据拿到可验证性)。

- **Time-R1(原 TimeZero,小米,2503.13377)**:纯 GRPO + format/IoU reward 做 temporal grounding;涌现出"为片段边界辩护"的 CoT;Charades-STA SOTA,首个超专用模型。
- **Tempo-R0(2507.04702)/ TempSamp-R1(2509.18056)**:高效时序感知 RL / 学"该采哪几帧"的时序采样。
- **共性**:grounding 是"最容易拿到可验证时序 reward"的任务形态 → RL 在此最稳(也因此对你 OPD 是"优势最弱"的格子,见你 `video-任务全景` §5)。

---

## 5. 机制四:grounded / 过程一致性 reward(防"不看画面编故事")

时序 CoT 的通病:推理链**不真的看画面**,编个看似合理的时序叙事。这一族**奖励"推理钉在时序视觉证据上"**。

**VideoRFT(2505.12434)语义一致性 reward**:

```
R_s = min( 1, w · max(cos(t, v), 0) ),  w=2
   t = SigLIP_text( 推理中"video describing"段的 M 个 token )
   v = (1/F) Σ_i SigLIP_image( 采样帧 v_i )        # F 帧嵌入均值
总 reward   R = R_f + R_a + 𝟙[R_a>0] · R_s          # 答对(R_a>0)才激活 R_s
```

**为何 gate 在 R_a>0**:避免"强化语义上合理但事实错误"的推理。**效果**:把描述性推理拉回视觉证据 → 减幻觉、增时序 grounded 性。

**MOSS-ChatV(2509.21113)过程推理 reward**:用**过程级**(评中间时序推理步)而非仅末端 reward,为"推理轨迹是否正确追踪了时序事件/顺序"提供 dense 信号。→ 这是六类里**最接近"过程 reward"**的,直通 §8 的 OPD。

---

## 6. 机制五:时序负例 / 自监督(把时序结构变成可自动构造的监督)

**思路**:时序有个便利——**"打乱/倒放/扰动"能自动造出干净负例**,不需人工标注。

- **TPO(Temporal Preference Optimization,2501.13919)**:DPO,负例 = 时序扰动(乱序/倒放)版本 → 学"偏好时序连贯的回答",面向长视频。
- **Synthetic Preference(2510.03955)**:自动造 乱序事件 / 倒序 负偏好,灌输时序依赖。
- **VideoSSR(2511.06281)自监督 RL**:任务 = **定位被扰动的时序段(anomaly grounding)** + 计数 → reward 来自"扰动位置已知",自监督即可验证。
- **共性**:与 SynRL 同宗——**靠"可自动构造 + 可验证"拿到干净时序信号**,只是 SynRL 走 verifiable-reward GRPO,这族走 preference/self-supervised。

---

## 7. 机制六:细粒度 credit assignment(把 credit 给"用了时序证据"的 token)

前五类改的是 **reward 来源**;这一类改的是 **reward 怎么摊到 token**——正是多步时序推理的 credit assignment 痛点(也是你 OPD 的主战场)。

- **TW-GRPO(2505.24718)**:**token 加权** GRPO,高信息 token 主导 loss + 软多选 reward → 低方差、聚焦。
- **Video-KTR(2601.19686)**:**key-token 归因**,把 credit 定位到"引用时序证据"的关键 token。
- **意义**:GRPO 把末端标量 advantage 均摊给所有 token(噪声大);这族让"真正做了时序推理的步"拿到更准的 credit。**这与 OPD 的 dense per-token 信号是同一个目标的两种手段**(§8)。

---

## 8. ★ OPD 视角:把 sparse outcome reward 换成 teacher 的 dense 信号

**统一视角**(接你 `video-任务全景` §5):OPD **就是 RL**,只是把 reward 来源从 verifier 换成 **teacher 的 per-token reverse-KL**(`r_t = log[p_T/p_S]`)。放到本篇框架:

- **OPD = 机制六的极致 + 机制四的极致**:teacher 的逐 token 信号 = 天然的 **dense 过程 reward + 完美 credit assignment**——它逐步告诉学生"这一步时序推理错多少",是 §5/§7 想做而做不满的事,OPD 结构上直接做到。
- **但 SynRL 的第一性洞察原样搬到 OPD**——**RL 与 OPD 提升时序的瓶颈,都是"监督信号的可靠性"**:

| | 信号来源 | 可靠性瓶颈 | 谁来保证 |
|---|---|---|---|
| **RLVR(SynRL)** | verifier / GT | reward 是否正确 | **合成 verifiable 数据** |
| **T-GRPO** | 正序-乱序对比 | 对比信号是否真指向时序 | 软阈值,较弱 |
| **OPD(你)** | teacher per-token KL | **teacher 在时序推理深处是否可靠** | **teacher-at-depth 门控(你的 contribution)** |

**给你的两个直接推论**:
1. **你的 contribution 与 SynRL 同构定位**:不是换算法,而是**保证时序推理监督信号的可靠性**。SynRL 靠"合成 verifiable"保证 reward 正确;你靠"teacher-at-depth 门控/校准"保证 teacher 信号在长时序 CoT 深处不漂。**这条类比可直接写进 motivation**(有 SynRL 这篇 2026 强证据背书"信号可靠性 > 数据量")。
2. **一个可借的点子**:SynRL 的**合成 verifiable 数据可给 OPD 当"teacher 可靠性锚"**——在有 GT 的合成时序段上,把 teacher 的 per-token 信号与 ground-truth 对照,**校准/门控 teacher**(teacher 在合成段偏离 GT 处 = 不可信,降权)。这把"合成可验证"从 RLVR 的 reward 来源,升级成 OPD 的 teacher-校准信号。

---

## 9. 一句话小结

**RL 提升时序理解 = ①让 reward 只在"真用了时序"时最大化(对比/grounding/一致性/负例)+ ②保证这个信号可靠(SynRL 的合成 verifiable 是最干净的证明)+ ③把信号摊到正确的时序推理步(credit assignment)。** OPD 是把 ② 的"信号来源"换成 teacher、把 ③ 做到 per-token 的极致——但它继承了 ② 的全部风险(teacher-at-depth 可靠性),这正是你要解、也最能对标 SynRL 的地方。

---

## 附:本篇引用的 RL 方法一览

| 方法 · arXiv | 机制类 | 时序 reward 核心 |
|---|---|---|
| **SynRL** · 2603.17693 | 一 | 合成 verifiable GRPO(二元准确率,完美 GT) |
| **Video-R1(T-GRPO)** · 2503.21776 | 二 | `r_t=α𝟙[p>μp̃]` 正序 vs 乱序 |
| **Time-R1 / TimeZero** · 2503.13377 | 三 | format + tIoU |
| Tempo-R0 · 2507.04702 / TempSamp-R1 · 2509.18056 | 三 | 时序感知 RL / 学采样 |
| **VideoRFT** · 2505.12434 | 四 | `R_s=min(1,2·max(cos(t,v),0))` 语义一致性 |
| MOSS-ChatV · 2509.21113 | 四 | 过程推理 reward(dense 中间步) |
| TPO · 2501.13919 / Synthetic-Pref · 2510.03955 | 五 | 时序扰动负例(DPO/偏好) |
| VideoSSR · 2511.06281 | 五 | 自监督:定位被扰动时序段 |
| TW-GRPO · 2505.24718 / Video-KTR · 2601.19686 | 六 | token 加权 / key-token 归因 |
| VISD(自蒸馏)/ Video-OPD · 见 video-推理/感知任务 | OPD | teacher per-token KL(你要超的天花板) |
